#!/usr/local/bin/task --taskfile

# https://taskfile.dev

version: "3"
vars:
  conda https://repo.anaconda.com/miniconda/Miniconda3-latest-MacOSX-arm64.sh
  
tasks:

  install-aws-cli:
    desc: _
    dir: __tmp__
    cmds: